{
  "private": true,
  "name": "client",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-preact": "^1.3.0",
    "jest": "^29.0.0",
    "sirv-cli": "^2.0.0"
  },
  "dependencies": {
    "preact-cli": "^3.4.1",
    "@picocss/pico": "^1.5.3",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "preact-router": "^4.0.0"
  }
}
