{
  "private": true,
  "name": "Decide",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-sw --no-prerender",
    "analyze": "preact build --analyze --no-sw --no-prerender",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "preact"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-preact": "^1.3.0",
    "jest": "^29.0.0",
    "sirv-cli": "^2.0.0"
  },
  "dependencies": {
    "preact-cli": "^3.4.1",
    "simpledotcss": "^2.1.1",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "preact-router": "^4.0.0"
  }
}
